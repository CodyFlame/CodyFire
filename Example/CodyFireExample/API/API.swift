class API {
    typealias post = PostController
    typealias comment = CommentController
    typealias album = AlbumController
    typealias photo = PhotoController
    typealias todo = TodoController
    typealias user = UserController
}
